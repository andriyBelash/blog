export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}

export enum Tokens {
  ACCESS = 'access_token',
  REFRESH = 'refresh_refresh',
}

export enum ArticleStatus {
  PUBLISHED = 'published',
  DRAFT = 'draft',
}